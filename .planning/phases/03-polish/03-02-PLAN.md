---
phase: 03-polish
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [templates/index.html, static/js/app.js, static/css/style.css]
autonomous: true
requirements: [EDGE-07]
user_setup: []
must_haves:
  truths:
    - "供应商/模型输入使用文本输入框而非下拉选择框，用户直接输入 ID 和名称"
    - "页面加载时自动检查 BiliNote 连接状态，断连时显示横幅提示"
    - "未选择文件时页面显示 4 步引导说明（Bilishelf 导出 -> 选择文件 -> 输入配置 -> 开始处理）"
    - "submitVideo 从文本输入框读取 provider_id 和 model_name"
    - "前端不再调用 /api/providers 和 /api/providers/{id}/models"
  artifacts:
    - path: "templates/index.html"
      provides: "文本输入框 + 空状态引导"
      contains: "provider-input, model-input, empty-state 步骤列表"
    - path: "static/js/app.js"
      provides: "移除 provider 加载逻辑，新增连接检测，文本输入读取"
      min_lines: 280
      contains: "checkBiliNoteConnection, providerInput.value, modelInput.value"
    - path: "static/css/style.css"
      provides: "文本输入框样式（与现有 select 一致）+ 引导步骤样式"
      contains: ".guide-steps, .guide-step"
  key_links:
    - from: "checkBiliNoteConnection() JS"
      to: "GET /api/check-connection"
      via: "fetch on page load + file load"
      pattern: "/api/check-connection"
    - from: "submitVideo() JS"
      to: "providerInput.value / modelInput.value"
      via: "文本输入框 value 而非下拉选中值"
      pattern: "providerInput\\.value|modelInput\\.value"
    - from: "DOMContentLoaded"
      to: "checkBiliNoteConnection()"
      via: "页面初始化时调用"
      pattern: "checkBiliNoteConnection"
---

<objective>
将供应商/模型从 API 驱动下拉框改为文本输入框（因为 BiliNote 无公开 /api/providers 端点），并在首页无文件时显示 4 步引导说明（EDGE-07）。

Purpose: 适配真实的 BiliNote API（无可枚举的供应商列表端点），以及降低新用户上手门槛——空状态时明确告知"先做什么、再做什么"。

Output: index.html（配置区改为文本输入 + 空状态引导）、app.js（移除 provider 加载，加入连接检测）、style.css（新元素样式）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-polish/03-CONTEXT.md
@.planning/phases/03-polish/03-01-PLAN.md
@.planning/REQUIREMENTS.md
@templates/index.html
@static/js/app.js
@static/css/style.css

<interfaces>
<!-- 后端 API 端点（Plan 01 已修复） -->
GET /api/check-connection → {connected: bool}  # 使用 /api/task_status/test
POST /api/process-video → 接受 {bvid, title, folder, url, model_name, provider_id, style, extras} → {task_id} / {error}

现有 DOM 结构 (index.html):
- .config-section → 包含两行 config-row:
  - 行 1: #provider-select + #model-select（将被替换为文本输入框）
  - 行 2: #style-select + #prompt-input（不变）
- .video-section → #video-list → .empty-state（将被更新为步骤引导）
- #banner → 连接状态横幅

现有 JS (app.js) 函数签名:
- loadFile(file) — 文件加载流程
- loadProviders() — 将被移除
- onProviderChanged() — 将被移除
- loadModels(providerId) — 将被移除
- renderVideoList(videos) — 不动
- submitVideo(video) — 需要更新 provider/model 取值
- startBatch() — 不动
- pollTaskStatus(taskId) — 不动
- checkBiliNoteConnection() — 新增
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 供应商/模型从下拉改为文本输入，移除 provider 加载逻辑</name>
  <files>templates/index.html, static/js/app.js</files>
  <read_first>templates/index.html（完整约 100 行）、static/js/app.js（完整约 411 行）</read_first>
  <action>
    将供应商和模型选择从 API 驱动下拉框改为用户直接输入文本，移除所有 provider/model API 加载代码。

    === index.html 修改 ===

    在 config-section 的第一个 config-row 中:

    1. 将:
    ```html
    <div class="config-group">
        <label for="provider-select">供应商</label>
        <select id="provider-select">
            <option value="">-- 请先加载文件 --</option>
        </select>
    </div>
    ```

    替换为:
    ```html
    <div class="config-group">
        <label for="provider-input">供应商 ID</label>
        <input type="text" id="provider-input" placeholder="如 openai" class="text-input">
    </div>
    ```

    2. 将:
    ```html
    <div class="config-group">
        <label for="model-select">模型</label>
        <select id="model-select" disabled>
            <option value="">-- 请先选择供应商 --</option>
        </select>
    </div>
    ```

    替换为:
    ```html
    <div class="config-group">
        <label for="model-input">模型名称</label>
        <input type="text" id="model-input" placeholder="如 gpt-4o" class="text-input">
    </div>
    ```

    === app.js 修改 ===

    1. state 对象（约第 7-12 行）移除 providers 和 models 字段:
       将 `providers: [],` 和 `models: [],` 从 state 中删除。
       最终 state = { videos: [], selectedFile: null, running: false, stopRequested: false, processedBvids: new Set(), currentIndex: 0, stats: { total: 0, success: 0, failed: 0, skipped: 0 }, outputDir: "./output" }

    2. DOM 引用（约第 21-27 行）:
       移除 `let providerSelect, modelSelect;`
       新增 `let providerInput, modelInput;`

    3. DOMContentLoaded 事件（约第 30-57 行）:
       - 移除: `providerSelect = $("provider-select");` 和 `modelSelect = $("model-select");`
       - 新增: `providerInput = $("provider-input");` 和 `modelInput = $("model-input");`
       - 移除: `providerSelect.addEventListener("change", onProviderChanged);`
       - 在 `promptInput = $("prompt-input");` 之后新增:
         ```javascript
         checkBiliNoteConnection();
         ```

    4. 删除整个 `onProviderChanged()` 函数（约第 103-113 行）。

    5. 将 `loadProviders()` 函数替换为 `checkBiliNoteConnection()`:
       删除原来的 `loadProviders()` 函数（约第 115-149 行）。
       在原位置插入新函数:
       ```javascript
       async function checkBiliNoteConnection() {
           try {
               const res = await fetch("/api/check-connection");
               const data = await res.json();
               if (!data.connected) {
                   showBanner("无法连接 BiliNote，请确认服务已启动", "warning");
               } else {
                   hideBanner();
               }
           } catch (err) {
               showBanner("无法连接 BiliNote：" + err.message, "warning");
           }
       }
       ```

    6. 删除整个 `loadModels(providerId)` 函数（约第 151-189 行）。

    7. 修改 `loadFile()` 函数（约第 70-100 行）:
       找到 `await loadCheckpoint(); renderVideoList(data.videos); hideBanner(); await loadProviders();` 这几行，
       替换为:
       ```javascript
       await loadCheckpoint();
       renderVideoList(data.videos);
       checkBiliNoteConnection();
       ```
       (注意: 原来的 `hideBanner()` 被移除，因为 `checkBiliNoteConnection()` 内部会管理横幅。如果连接正常则隐藏，否则显示警告。)

    8. 修改 `submitVideo()` 函数（约第 290-314 行）:
       将 `model_name: modelSelect.value` 改为 `model_name: modelInput.value`
       将 `provider_id: providerSelect.value` 改为 `provider_id: providerInput.value`
       注意: 保持提交字段名不变（model_name, provider_id），只改变取值 source。

    修改后效果:
    - 页面上供应商和模型从下拉框变为 <input type="text">
    - 用户直接输入供应商 ID（如 openai）和模型名称（如 gpt-4o）
    - 页面加载时自动检查 BiliNote 连接
    - 文件加载后也检查连接
    - submitVideo 从文本输入框取值
  </action>
  <verify>
    <automated>cd D:/1_RSYHNHLN_Files/competition/bilinote-batch-tool && python -c "
with open('templates/index.html') as f: html = f.read()
assert 'provider-input' in html, 'Missing provider-input'
assert 'model-input' in html, 'Missing model-input'
assert 'provider-select' not in html, 'Stale provider-select'
assert 'model-select' not in html, 'Stale model-select'
print('HTML provider/model inputs: OK')
" && python -c "
with open('static/js/app.js') as f: js = f.read()
assert 'providerInput' in js, 'Missing providerInput'
assert 'modelInput' in js, 'Missing modelInput'
assert 'checkBiliNoteConnection' in js, 'Missing checkBiliNoteConnection'
assert 'loadProviders' not in js, 'Stale loadProviders'
assert 'onProviderChanged' not in js, 'Stale onProviderChanged'
assert 'loadModels' not in js, 'Stale loadModels'
assert 'providerSelect' not in js, 'Stale providerSelect'
assert 'modelSelect' not in js, 'Stale modelSelect'
assert 'providerInput.value' in js or 'modelInput.value' in js, 'Missing text input value reads'
print('JS provider/model text input: OK')
"</automated>
  </verify>
  <done>
    - index.html 中 provider-select 和 model-select 被替换为 provider-input 和 model-input（type="text"）
    - app.js 中 loadProviders、loadModels、onProviderChanged 三个函数被彻底移除
    - app.js 新增 checkBiliNoteConnection 函数，在页面加载和文件加载后调用
    - submitVideo 从 providerInput.value 和 modelInput.value 取值
    - state 中 providers 和 models 字段已移除
  </done>
</task>

<task type="auto">
  <name>Task 2: 空状态引导说明 (EDGE-07)</name>
  <files>templates/index.html, static/css/style.css</files>
  <read_first>templates/index.html（已包含 Task 1 的文本输入框）、static/css/style.css（完整 CSS）</read_first>
  <action>
    将未选择文件时的空状态从简单文字改为 4 步引导说明（per D-02）。

    === index.html 修改 ===

    在 video-list 中（约第 83 行），将:
    ```html
    <div class="empty-state">请先选择收藏文件</div>
    ```

    替换为:
    ```html
    <div class="empty-state">
        <div class="guide-title">&#x1F4CB; 使用步骤</div>
        <ol class="guide-steps">
            <li class="guide-step">从 <strong>Bilishelf</strong> 导出收藏文件（JSON 或 CSV 格式）</li>
            <li class="guide-step">点击上方 <strong>"选择收藏文件"</strong> 按钮加载导出的文件</li>
            <li class="guide-step">输入 <strong>供应商 ID</strong> 和 <strong>模型名称</strong>，选择 <strong>笔记风格</strong></li>
            <li class="guide-step">点击 <strong>"开始批量处理"</strong> 自动逐个生成笔记</li>
        </ol>
        <p class="guide-note">已处理视频会自动跳过，支持断点续传</p>
    </div>
    ```

    === style.css 修改 ===

    在 `.empty-state` 样式块（约第 374-380 行）之后追加:

    ```css
    /* ── 空状态引导步骤 ──────────────────────────────────── */
    .guide-title {
        font-size: 1.0625rem;
        font-weight: 600;
        color: var(--text-primary);
        margin-bottom: var(--spacing-md);
    }

    .guide-steps {
        text-align: left;
        max-width: 420px;
        margin: 0 auto;
        padding-left: 20px;
        line-height: 1.8;
    }

    .guide-step {
        margin-bottom: var(--spacing-sm);
        color: var(--text-secondary);
        font-size: 0.9rem;
    }

    .guide-step strong {
        color: var(--text-primary);
    }

    .guide-note {
        margin-top: var(--spacing-md);
        color: var(--text-muted);
        font-size: 0.8125rem;
    }
    ```

    注意: 引导说明中的图标（&#x1F4CB;）仅用于标题装饰。所有文字使用纯文本，无 emoji。
  </action>
  <verify>
    <automated>cd D:/1_RSYHNHLN_Files/competition/bilinote-batch-tool && python -c "
with open('templates/index.html') as f: html = f.read()
assert '使用步骤' in html, 'Missing guide title'
assert 'Bilishelf' in html, 'Missing Bilishelf step'
assert '选择收藏文件' in html, 'Missing select file step'
assert '供应商 ID' in html, 'Missing provider step'
assert '开始批量处理' in html, 'Missing start step'
print('HTML guide steps: OK')
" && python -c "
with open('static/css/style.css') as f: css = f.read()
assert '.guide-title' in css and '.guide-steps' in css and '.guide-step' in css and '.guide-note' in css
print('CSS guide styles: OK')
"</automated>
  </verify>
  <done>
    - 未选择文件时 video-list 显示 4 步引导说明（从 Bilishelf 导出 -> 选择文件 -> 输入配置 -> 开始处理）
    - 引导使用有序列表，步骤清晰
    - 底部提示"已处理视频会自动跳过，支持断点续传"
    - CSS 中 .guide-title / .guide-steps / .guide-step / .guide-note 样式存在
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器页面 -> 用户 | 引导步骤和文本输入框展示给用户 |
| 浏览器 JS -> Flask API | fetch 到 localhost:8765 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-FE-01 | Tampering | 引导步骤文字 XSS | accept | 引导文字硬编码在 HTML 中，无用户输入，无注入风险 |
| T-03-FE-02 | Spoofing | 文本输入代替下拉 | accept | 用户手动输入供应商 ID 和模型名，不支持自动补全验证（BiliNote 无列表 API）|
| T-03-FE-03 | Denial of Service | 空 provider_id 提交 | mitigate | BiliNote 后端负责验证必填字段，前端不做额外校验 |
| T-03-SC | Tampering | 无新包依赖 | accept | 零新依赖 |</threat_model>

<verification>

### Automated Verification
```bash
cd D:/1_RSYHNHLN_Files/competition/bilinote-batch-tool
python -c "with open('templates/index.html') as f: c=f.read(); assert 'provider-input' in c and 'model-input' in c and '使用步骤' in c"
python -c "with open('static/js/app.js') as f: c=f.read(); assert 'checkBiliNoteConnection' in c and 'loadProviders' not in c"
python -c "with open('static/css/style.css') as f: c=f.read(); assert '.guide-steps' in c"
```

### Manual Verification (post-execution, requires Plan 01 + Plan 02 both complete)
1. `python app.py` 启动，打开浏览器
2. 未选择文件时页面显示 4 步引导说明，无供应商/模型下拉（有文本输入框）
3. 供应商输入框 placeholder 显示"如 openai"
4. 模型输入框 placeholder 显示"如 gpt-4o"
5. 若 BiliNote 离线，顶部横幅显示"无法连接 BiliNote"
6. 加载文件后，视频列表正常显示，引导步骤消失

</verification>

<success_criteria>

- [ ] index.html 中 provider-select/model-select 完全移除，替换为 provider-input/model-input（type="text"）
- [ ] app.js 中 loadProviders、loadModels、onProviderChanged 完全移除
- [ ] app.js 新增 checkBiliNoteConnection 函数，页面加载和文件加载后自动调用
- [ ] submitVideo 从 providerInput.value 和 modelInput.value 读取供应商/模型
- [ ] 初始页面显示 4 步引导说明（EDGE-07 per D-02）
- [ ] CSS 包含 .guide-title / .guide-steps / .guide-step / .guide-note 样式
- [ ] 引导说明底部提示"已处理视频会自动跳过，支持断点续传"

</success_criteria>

<output>
创建 `.planning/phases/03-polish/03-02-SUMMARY.md` 当完成
</output>
